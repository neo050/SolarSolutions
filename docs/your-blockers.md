# מה חסום עליך

13 פריטים. לכל אחד: **מה בדיוק לעשות → מה לשלוח → מה משתחרר אצלנו.**

סדר מומלץ: 2 → 6 → 8/9/10 → 1 → 11/12 → השאר.
סעיף 2 הוא היחיד שמפסיד ערך על כל יום שעובר. סעיף 6 הוא שאלה של משפט אחד.

---

## איך לראות את האתר עכשיו, בלי לחכות לאירוח

לא צריך לחכות להחלטת ה-Hosting כדי לעבור על המוצר. הפקודה הבאה מגישה את **בדיוק אותה
בנייה** שתעלה לאוויר:

```
cd site
npm install     # פעם אחת בלבד
npm run build
npm run preview
```

ואז לפתוח את הכתובת שמודפסת (בדרך כלל `http://localhost:4321/`). כל 42 העמודים חיים
וניתנים לניווט, כולל הטפסים.

בנוסף — `site/qa-screenshots/` מכיל 168 צילומי מסך של כל עמוד בארבעה רוחבים
(390 / 820 / 1024 / 1440), שנוצרו אוטומטית ב-`npm run qa:visual`.

---

# חשבונות ותשתית

## 1 · אירוח (Hosting)

**למה חסום:** GitHub Pages מגיש קבצים סטטיים בלבד — הוא לא יכול להריץ את נקודת הקצה
ללידים ולא להנפיק 301 אמיתי. שניהם כתובים ומחכים.

**מה לעשות:** לפתוח פרויקט ב-Cloudflare Pages, לחבר לריפו, Root directory `site`,
Build `npm run build`, Output `dist`. ליצור KV namespace בשם `LEADS` ולקשר אותו
תחת Settings → Functions.

**מה לשלוח:** שם הפרויקט, כתובת ה-`*.pages.dev`, ולאיזה מייל שההתראה על ליד תגיע.

**מה משתחרר:** Staging ציבורי · הטופס עובר לשליחה לשרת עם אישור קליטה אמיתי במקום
מסירה לוואטסאפ · 13 ההפניות הופכות ל-301 · כותרות האבטחה נכנסות לתוקף · בדיקות E2E
מלאות לשלושת המסלולים.

## 2 · GA4 + GTM ← **השבוע**

**למה דחוף:** עשרה אירועים כבר נכנסים לתור. חסר רק מזהה. כל שבוע בלי מדידה הוא שבוע של
נתוני בסיס שאי אפשר לשחזר — ובלעדיהם אין דרך להוכיח שהאתר החדש שיפר משהו.

**מה לעשות:** ליצור נכס ב-analytics.google.com ולהעתיק את ה-Measurement ID
(`G-XXXXXXXXXX`). ליצור מכולה ב-tagmanager.google.com ולהעתיק את ה-Container ID
(`GTM-XXXXXXX`). במכולה: Tag חדש → GA4 Configuration → Trigger: All Pages → **Submit**.

**מה לשלוח:** שני המזהים.

**מה משתחרר:** מדידה על האתר **הקיים** מיד · הגדרת עשרת האירועים · סימון ההמרות ·
אימות ב-DebugView.

## 3 · Google Business Profile

**למה חסום:** הדירוג המפוברק הוסר. Review schema הוסר לגמרי, כי גוגל דורש `reviewRating`
ואף אחת מחמש ההמלצות לא נושאת דירוג — להמציא כוכבים זו אותה המצאה שהסרנו.

**מה לעשות:** לוודא שקיים פרויל ושאתה בעל הרשאה. לוודא ששם, כתובת וטלפון זהים בדיוק
לאתר.

**מה לשלוח:** קישור לפרופיל + מספר הביקורות והדירוג בפועל.

**מה משתחרר:** דירוג אמיתי בעמוד ההמלצות · `sameAs` ב-Schema · Review markup חוזר ·
Local SEO מחובר.

## 4 · הגבלת מפתח Google Maps

**מה לעשות:** ב-console.cloud.google.com/apis/credentials → המפתח שמתחיל ב-`AIzaSyCgEhKy…`
→ Application restrictions → Websites → `https://rnrg.co.il/*` ו-`https://www.rnrg.co.il/*`
→ API restrictions → רק Maps JavaScript API ו-Places API. להגדיר התראת חיוב.

**מה לשלוח:** אישור שההגבלה הוגדרה.

**מה משתחרר:** סגירת פריט אבטחה P0 · השלמת כתובת אוטומטית בטופס הסולארי.

## 5 · הדומיין הישן

**מה לעשות:** לפתוח `neo050.github.io/SolarSolutions/` — נטען או 404? לחפש בגוגל
`site:neo050.github.io`. אם חי — Settings → Pages → Source: None.

**מה לשלוח:** האם חי, וכמה עמודים מאונדקסים.

**מה משתחרר:** 301 מהדומיין הישן · ניקוי תוכן כפול לפני העלייה.

---

# בירורים על חומרים קיימים

## 6 · התמונה עם החשמלאי האחר ← **משפט אחד**

**הרקע:** התמונה ששימשה כתמונה הראשית של דף הבית ושל עמוד הקבלנות כוללת מדבקה:
"חשמלאי מוסמך · רוני בורוכוב · 058-5442623". הוצאה לבידוד.

**מה לשלוח:** האם זו עבודה שלכם בלוח שכבר נשא מדבקה של מי שהיה שם קודם, או שהתמונה
הגיעה ממקור אחר?

**מה משתחרר:** אם שלכם — אפשר לצלם מחדש בלי המדבקה. אם לא — היא נמחקת סופית ואנחנו
יודעים לבדוק את שאר החומרים ביתר חשד.

## 7 · אישור שימוש בתמונות הלקוח המסחרי

**הרקע:** ארבע תמונות מתעדות פרויקט תאורה מסחרי אמיתי — משלב תקרת גבס פתוחה ועד גמר.
זו ההוכחה המסחרית החזקה ביותר בספרייה. שילוט המותג של הלקוח גלוי בהן.

**מה לעשות:** לבקש מבעל העסק אישור לפרסם את התמונות ולציין את שמו. הודעת וואטסאפ מספיקה.

**מה משתחרר:** Case Study מסחרי מלא · שם לקוח מזוהה כ-Trust signal · הסרת ה-Pending
מעמוד המסחרי.

---

# חומרים לצילום

## 8 · תמונות של רוני ← **הפריט החוסם ביותר**

**למה:** אין באתר ולו תמונה אחת של אדם. אתר שירות בלי פנים לא מייצר אמון, ובענף הקבלני
זה קריטי.

**מה לשלוח:** 3–5 תמונות מזוויות שונות, ביגוד עבודה, סביבה אמיתית, לפחות 1600px.
להעלות ל-`AI-ASSET-PACK/SOURCE-ASSETS/PERSON/` בשמות `SRC-RONI-001-a.jpg` וכו׳.

**מה משתחרר:** `GEN-PERSON-PORTRAIT-001` — ה-Prompt כבר כתוב וממתין. מהתמונות שלך אפשר
לייצר וריאציות, זוויות וסצנות נוספות לעמודי השירות ול-CTA.

## 9 · תמונות צוות

**מה לשלוח:** צוות בעבודה — פנים, ביגוד, ציוד, סביבה. ל-`SOURCE-ASSETS/TEAM/`.

**מה משתחרר:** `GEN-TEAM-ONSITE-001`. הערה אחת: מספר האנשים בתמונות שייווצרו יתאים
למספר האמיתי (סעיף 11) — תמונה של חמישה כשיש שניים היא התחייבות מול קבלן ששואל כמה
אנשים מגיעים לאתר.

## 10 · תמונות עבודות

**מה לשלוח:**
- אתר בנייה **למגורים** בשלב שלד — צנרת, שרוולים, קופסאות לפני יציקה
  (שלב תשתית מסחרי כבר מתועד). ל-`SOURCE-ASSETS/CONTRACTING/`.
- התקנה סולארית **תוך כדי עבודה** — כל 12 תצלומי הסולארי הם מערכות גמורות.
  ל-`SOURCE-ASSETS/SOLAR/`.
- 2–3 לוחות מסחריים נוספים.

**מה משתחרר:** `GEN-CONTRACTING-SITE-001`, `GEN-SOLAR-INSTALL-001`, וראיות נוספות
לעמודי הקבלנות.

---

# עובדות עסקיות

## 11 · שיחה אחת עם רוני — הנתונים החסרים

הרשימה המלאה מיוצרת אוטומטית ומופיעה ב-`/internal/gaps/` באתר. בקצרה:

| נתון | למה זה חוסם |
|---|---|
| שנת תחילת העיסוק בחשמל, בנפרד מ-2018 | הטענה הישנה "מעל 8 שנות ניסיון" סתרה את עמוד האודות |
| מספר אנשי צוות — קבועים ולפי פרויקט | השאלה הראשונה שקבלן שואל |
| הפרויקט הגדול ביותר שבוצע | מסנן התאמה ליזם |
| כמה פרויקטים במקביל | התנגדות נפוצה של מנהל פרויקט |
| ביטוח עבודות קבלניות וצד ג׳ | תנאי סף אצל כל קבלן ראשי מסודר |
| תנאי אחריות — שנים ומה מכוסה | "אחריות לשנים קדימה" בלי מספר לא שווה כלום |
| רשימת יישובים בפועל | בלעדיה עמודי אזור יהיו תוכן דל, ולכן לא נבנו |
| רישום בפנקס הקבלנים — כן/לא, ענף וסיווג | תיאור מסחרי ≠ דרגת רישיון. אסור לרמוז על רישום שאינו קיים |
| הסמכות מעבר לרישיון 991433 | Authority ותנאי סף |
| שעות פעילות · זמן תגובה ממוצע | Local SEO והמרה |
| באיזה מסלול אסדרה סולארית מגישים בפועל | "מסלול ירוק" אינו השם הרגולטורי היום |

## 12 · נתוני Case Studies

**מה לשלוח:** לחמשת הפרויקטים המובילים — האתגר, הפתרון, משך הביצוע, גודל הצוות,
התוצאה, ואישור לפרסום שם הלקוח. ובנוסף: **פרויקט קבלני אחד** — כל עשרת המתועדים
סולאריים, ואין ולו הוכחה אחת לענף שהוא מרכז הפרויקט.

**מה משתחרר:** עמודי ה-Case Study כבר בנויים ומחכים — המבנה, העיצוב וה-URL קיימים.
מילוי שלושה שדות בקובץ אחד הופך כל עמוד מגלריה ל-Case Study מלא, בלי שינוי קוד או עיצוב.

## 13 · אישורים נוספים שיידרשו

לוגואים של קבלנים או חברות שמותר להציג · המלצה אחת מלקוח עסקי · קובץ לוגו מקורי
בווקטור (הקיים הוא רסטר עטוף, 494KB).

---

## מה אנחנו עושים בינתיים

ממשיכים בכל מה שלא תלוי בך. הרשימה המלאה: [`docs/task-board.md`](task-board.md).
